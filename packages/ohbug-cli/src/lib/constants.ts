/**
 * Log prefix for CLI output
 * CLI 输出的日志前缀
 */
export const LOG_PREFIX = "[OhbugCli]";

/**
 * Request timeout in milliseconds
 * 请求超时时间（毫秒）
 */
export const TIMEOUT = 30000;

/**
 * Default endpoint for source map upload
 * 上传 source map 的默认端点
 */
export const DEFAULT_ENDPOINT = "http://localhost:6660/sourceMap/upload";
